import '../../../utils/tools/importer.dart';

class CategoryPage extends StatelessWidget {
  final int position;
  const CategoryPage({super.key, required this.position});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
